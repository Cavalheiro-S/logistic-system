export enum PermissionEnum {
    VIEW = "VIEW",
    CREATE = "CREATE",
    UPDATE = "UPDATE",
    DELETE = "DELETE",
}

export enum PermissionEntities {
    DRIVER = "DRIVER",
    USER = "USER",
    PRODUCT = "PRODUCT",
    ORDER = "ORDER",
    VEHICLE = "VEHICLE",
}